var Candidate = {
  attributes: {
    // e.g., "John doe"
    name: {
      type: 'string'
    },

    // e.g., "john@doe.com"
    email: {
      type: 'string'
    }
  }
};

module.exports = Candidate;